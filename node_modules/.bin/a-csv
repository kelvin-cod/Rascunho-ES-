#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../a-csv/bin/a-csv" "$@"
  ret=$?
else 
  node  "$basedir/../a-csv/bin/a-csv" "$@"
  ret=$?
fi
exit $ret
